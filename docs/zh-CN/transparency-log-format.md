# 透明日志 schema 1

> 英文原文：[Transparency log schema 1](../transparency-log-format.md)

透明日志保存经审查部署锚和对待执行命令的独立签名观测。记录形成确定性 Merkle 树，并可生成签名检查点、包含证明和前缀一致性见证。

发布使用预期 head；加载限制记录、证明、树节点与字节数，并拒绝篡改、截断、分叉、符号链接和不一致 root。调用方必须固定可信签名 key 和外部检查点，才能检测完整日志回滚。

透明记录提高软件历史的可审计性，但不把观测者声明转换为控制器授权或物理执行事实。
