# 版本化车队调度历史

> 英文原文：[Versioned fleet-schedule archives](../fleet-schedule-archive.md)

`FleetScheduleArchive` 保存多机器人预留分析的确定性线性历史。每个版本绑定车队快照、安全记忆 ID、预留集合、冲突报告、父 ID 和序号。

发布使用预期 head，重复发布相同输入为幂等；不同输入必须形成正确后继。加载验证完整父链、报告重算、稳定排序、校验和、字节/版本/预留限制。

“在声明包络下无冲突”只反映存档输入。它不是调度租约、实时协调、控制器互锁或物理隔离证明。
